import { TtPlayerNameReducerPipe } from './tt-player-name-reducer.pipe';

describe('TtPlayerNameReducerPipe', () => {
  it('create an instance', () => {
    const pipe = new TtPlayerNameReducerPipe();
    expect(pipe).toBeTruthy();
  });
});
